version: '3.9'

services:
  db:
    image: postgres:16
    container_name: grades_db
    environment:
      POSTGRES_DB: grades_db
      POSTGRES_USER: grades_db
      POSTGRES_PASSWORD: grades_db
    ports:
      - "5430:5432"
